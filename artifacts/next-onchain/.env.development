# Local-dev-only defaults (safe to commit — no secrets). Next.js loads this
# automatically for `next dev`; it never applies to `next build`/production.

# Lets you hit /api/debug/vault-apys while developing locally.
ENABLE_DEBUG_ENDPOINT=true
